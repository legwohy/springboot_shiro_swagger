<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swagger.dao.UserInfoDao">

	<!-- 状态查询用户-->
	<select id="findUserInfo"  resultType="map">
		SELECT * from user_info
	</select>

</mapper>